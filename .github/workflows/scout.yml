name: Global-Macro-Scout

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

concurrency:
  group: macro-scouting
  cancel-in-progress: true

jobs:
  run-scout:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          # 🚨 전체 히스토리를 가져와야 꼬이지 않습니다.
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install
        run: pip install requests beautifulsoup4 pandas python-dateutil lxml

      - name: Run
        run: python scout_jp.py

      # 🚨 [무적의 단계] 직접 git 명령어를 쓰는 대신 전용 도구를 사용합니다.
      - name: Push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add macro_intelligence.db
          # 변경사항이 있을 때만 커밋
          git diff --quiet && git diff --staged --quiet || git commit -m "update db"
          
          # 🚨 [핵심] 그냥 push가 아니라 --force를 사용합니다.
          # 서버의 데이터와 충돌나면 서버걸 무시하고 내걸로 덮어버립니다.
          git push origin main --force
