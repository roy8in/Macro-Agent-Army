name: Global Macro Scouting

on:
  schedule:
    # 1시간마다 한 번씩 자동으로 실행 (UTC 기준)
    - cron: '0 */1 * * *'
  workflow_dispatch: # 수동 실행 버튼 활성화

jobs:
  build:
    runs-on: ubuntu-latest
    # 🚨 깃허브 액션이 DB를 저장할 수 있게 권한을 명시합니다.
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 pandas

      - name: Run Japan Scouter
        run: python scout_jp.py

      - name: Commit and Push DB
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add macro_intelligence.db
          git commit -m "🚀 자동 정찰 데이터 업데이트 ($(date +'%Y-%m-%d %H:%M'))" || echo "변경 사항 없음"
          git push